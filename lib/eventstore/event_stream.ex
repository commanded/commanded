defmodule EventStore.EventStream do
  def append(persistence, stream_uuid, expected_version, events) do
    
  end

  def read(persistence, stream_uuid) do
    
  end
  
  def read(persistence, stream_uuid, limit) do
    
  end

  def read(persistence, tream_uuid, from_version, limit) do
    
  end
end